---
export interface Props {
  noPadding?: boolean;
  ariaHidden?: boolean;
}

const { noPadding = false, ariaHidden = true } = Astro.props;
---

<div class={`max-w-4xl mx-auto ${noPadding ? "px-0" : "px-6 sm:px-8"}`}>
  <hr class="border-skin-line my-4" aria-hidden={ariaHidden} />
</div>
